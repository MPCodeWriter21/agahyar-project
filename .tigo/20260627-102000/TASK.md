# Set up GitHub Actions CI workflow
- STATUS: OPEN
- PRIORITY: 85
- TAGS: ci, infra

AGENTS.md specifies CI runs on GitHub Actions on Python 3.12. Create .github/workflows/ci.yml with: lint (ruff), test (pytest), on push/PR to main.
