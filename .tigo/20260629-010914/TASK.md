# Data management and backup

- STATUS: CLOSED
- PRIORITY: 55
- TAGS: data, ops

Implement data export, import, backup, and seed data.

- Create a management command to export all data (JSON/CSV)
- Create a management command to import data from export
- Add admin action for bulk export
- Create a backup script for the database
- Add seed data (sample services, FAQs, centers) for demo
- Document backup and restore procedures
