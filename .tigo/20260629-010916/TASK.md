# REST API for external consumption

- STATUS: OPEN
- PRIORITY: 50
- TAGS: api, feature

Create a REST API so external services and apps can consume the data.

- Add Django REST Framework dependency
- Create API endpoints for services, centers, FAQs
- Add authentication via tokens (DRF token auth or JWT)
- Add pagination, filtering, and search on API endpoints
- Version the API (e.g. /api/v1/)
- Document API with OpenAPI/Swagger (drf-spectacular or drf-yasg)
