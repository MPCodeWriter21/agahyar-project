# Refresh session

- STATUS: OPEN
- PRIORITY: 70
- TAGS: UX, auth, login, feature, session

Implement a refresh system that will let the users stay logged in longer.
If a user logs into their account and uses it for a full session length, they should not be logged out and have to be logged in again.
The session's life must be refreshed if the user keeps using it.
