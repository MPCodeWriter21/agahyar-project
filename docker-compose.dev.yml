services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./agahyar_project:/app/agahyar_project
      - ./services:/app/services
      - ./templates:/app/templates
      - ./static:/app/static
      - ./manage.py:/app/manage.py
